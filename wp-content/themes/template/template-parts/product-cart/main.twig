
<main>
    <div class="container">
        <div class="row" id="sticky-parent">
            <div class="col-lg-8">
                <div class="product-card">
                    <div class="row">
                        <div class="col-lg-8 col-sm-10 col-12">
                            <a href="{{ get_page_link(7) }}" class="link link_default product-card__link">Вернуться в
                                каталог</a>

                            <h1 class="product-card__title heading heading_default">
                                {{ title }}
                            </h1>
                            <div class="product-card__table">

                                {% if catalog_number %}
                                <div class="product-card__row">
                                    <div class="product-card__cell product-card__cell_title">Каталожный номер:
                                    </div>
                                    <div class="product-card__cell">{{ catalog_number }}</div>
                                </div>
                                {% endif %}

                                {% if marks %}
                                <div class="product-card__row">
                                    <div class="product-card__cell product-card__cell_title">
                                        Марка:
                                    </div>
                                    <div class="product-card__cell">{{ marks }}</div>
                                </div>
                                {% endif %}

                                {% if in_stock %}
                                <div class="product-card__row">
                                    <div class="product-card__cell product-card__cell_title">На
                                        складе:
                                    </div>
                                    <div class="product-card__cell">{{ in_stock }}</div>
                                </div>
                                {% endif %}

                                {% if categories %}
                                <div class="product-card__row">
                                    <div class="product-card__cell product-card__cell_title">
                                        Категория:
                                    </div>
                                    <div class="product-card__cell">{{ categories }}</div>
                                </div>
                                {% endif %}

                                <div class="product-card__row">
                                    <div class="product-card__cell product-card__cell_title">
                                        цена
                                    </div>
                                    <div class="product-card__cell">{{ price }}</div>
                                </div>

                                <div class="product-card__row">
                                    <div class="product-card__cell product-card__cell_title">
                                        количество
                                    </div>
                                    <div class="product-card__cell">
                                        <div class="d-flex justify-content-start">
                                            <div class="number-input flex-shrink-0">
                                                <input type="number" value="1" min="1" class="number-input__field">
                                                <button class="number-input__control number-input__control_minus"></button>
                                                <button class="number-input__control number-input__control_plus"></button>
                                            </div>

                                            <div class="btn btn_warn btn_md btn_cart add_to_basket" data-id="{{ id }}">
                                                отложить в заказ
                                            </div>

                                        </div>
                                    </div>
                                </div>
                            </div>

                            <a href="{{ get_page_link(7) }}" class="link link_default product-card__link">Вернуться в
                                каталог</a>
                        </div>
                    </div>
                </div>
            </div>
            <div class="col-lg-4">
                <aside class="sidebar block block_border product-page__sidebar" id="sidebar">
                    <section class="sidebar__section">
                        <h4 class="heading heading_default">
                            <small>Есть вопросы по этой запчасти?</small>
                        </h4>
                        <p class="question__text">Задайте их по телефону</p>
                        <a href="tel:{{ phone }}" class="phone-link">{{ phone }}</a>
                    </section>

                    <hr>

                    <section class="sidebar__section">
                        <h4 class="sidebar__title">Условия доставки</h4>
                        <ul class="unorder-list">
                            {% for item in conditions.shipping %}
                                <li class="unorder-list__item">{{ item.item }}</li>
                            {% endfor %}
                        </ul>
                    </section>
                    <hr>
                    <section class="sidebar__section">
                        <h4 class="sidebar__title">Условия оплаты</h4>
                        <ul class="unorder-list">
                            {% for item in conditions.payment %}
                                <li class="unorder-list__item">{{ item.item }}</li>
                            {% endfor %}
                        </ul>
                    </section>
                </aside>
            </div>
        </div>
    </div>
</main>